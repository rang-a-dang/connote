---
name: connote
description: Apply Connote (CDL) editing directives to a Markdown document.
disable-model-invocation: true
---

# Connote (placeholder)

This skill is a scaffold. The Connote Directive Language (CDL) and command set are
documented in the project README and `docs/Commands.md`; the edit engine is not
implemented yet.

When invoked today:

1. Tell the user Connote is a work in progress and not yet applying edits.
2. If the active Markdown document contains `co/` directives, list the directives
   you found (scope, command, and instructions) so the intended edits are visible.

Do not modify the document yet.
